#include <stdio.h>
#include <stdlib.h>

int main()
{
    int counter, num, largest, a,b,c,d,e;
    printf("Input 5 integer: ");
    scanf("%d %d %d %d %d", &a, &b, &c, &d, &e);

    for(counter=0 ; counter<=0 ; counter++)
    {

    }

    return 0;
}
